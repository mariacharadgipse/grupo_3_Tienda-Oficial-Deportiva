const controller = {
    productDetailController: (req, res) => {
     // Lógica del controlador para la página de inicio
  res.render('productDetail'); // Renderiza la plantilla 'productDetail.ejs' en la carpeta 'views'
}
};

module.exports=controller;