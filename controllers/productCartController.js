const controller = {
    productCart: (req, res) => {
     // Lógica del controlador para la página de inicio
  res.render('productCart'); // Renderiza la plantilla 'productCart.ejs' en la carpeta 'views'
}
};

module.exports=controller;