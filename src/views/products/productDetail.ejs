<!DOCTYPE html>
<html lang="en">

<%- include('../partials/head', { title: 'Detalle' , stylesheet: ['/css/styles.css','/css/productDetail.css']}) %>

    <body>
        <%- include('../partials/header') %>
            <h2 class="titulo">Producto Seleccionado</h2>
            <main>

                <div class="contenedorGris">

                    <div>
                        <img src="/img/products/<%= pSelected.image %>" alt="product image" class="product-detail-img"
                            width="100%" height="300 px">
                    </div>

                    <div class="product-detail">
                        <article class="product-detail-info">
                            <p class="product-detail-description">
                                <%= pSelected.description %>
                            </p>

                            <h2 class="product-detail-title">
                                <%= pSelected.name %>
                            </h2>

                            <p class="product-detail-price small">
                                <span>$<%= pSelected.price %></span>
                                <b>
                                    <%= pSelected.discount %>% OFF
                                </b>
                            </p>
                            <p class="product-detail-price">
                                $<%= pSelected.price %>
                                    <!-- falta final price (Calculo) -->
                            </p>

                            <ul class="actions-list">
                                <li>
                                    <i class="fas fa-credit-card"></i>
                                    <p><strong>Pagá en 12 cuotas sin interes</p></strong>
                                </li>
                                <li>
                                    <i class="fas fa-store"></i>
                                    <p><strong>Retiro gratis en locales del vendedor</p></strong>
                                </li>
                            </ul>
                            <div class="boton"></div>
                            <a href="/products/cart/<%= pSelected.id %>" class="boton"><button type="submit" class="boton">COMPRAR</button></a>
                        </div>
                            
                        </article>
                        
                    </div>

                </div>
                <div class="adm">
                            <a href="/products/edit/<%= pSelected.id %>" class="enlace"><button type="submit" class="boton">EDITAR</button></a>
                            <form action="/products/delete/<%= pSelected.id %>?_method=DELETE" method="POST"
                                    style="display: inline-flex"> <button type="submit" class="boton">ELIMINAR</button>
                            </form>
                        </div>
            </main>

            <%- include('../partials/footer') %>
    </body>

</html>